# Skill 强制修复提示词 v3（终极防作弊版）

> 复制以下全部内容发送给 Claude

---

```
# 任务：修复 prompt-optimizer skill

## ⛔ 防作弊规则

1. Step 2 必须**完整复制** Step 1 的每一个字符，包括 `<tag>` 和 `</tag>`
2. 禁止在 Step 2 省略任何内容
3. Step 1 和 Step 2 的字符数必须一致

---

## Step 1: 生成提示词

需求："生成 API 测试用例"

生成完整提示词。

### 格式要求

必须包含以下 7 个标签，且每个标签必须闭合：

```
<role>...</role>
<goal>...</goal>
<input>...</input>
<output_format>...</output_format>
<constraints>...</constraints>
<error_handling>...</error_handling>
<examples>...</examples>
```

### 输出提示词（用代码块包裹）

```xml
在这里输出完整提示词
```

---

## Step 2: 逐行编号（完整复制）

⚠️ 必须完整复制 Step 1 的内容，包括所有 `<tag>` 和 `</tag>`

格式：
```
001: <role>
002: 你是一位资深 QA 工程师...
003: </role>
004: 
005: <goal>
...
```

⛔ 禁止：
- 禁止省略 `<tag>` 或 `</tag>`
- 禁止把多行合并成一行
- 禁止显示空行而不显示标签内容

---

## Step 3: 验证（必须在 Step 2 中能找到对应行）

对于每个检查项，在 Step 2 的编号内容中找到对应行：

| # | 搜索内容 | Step 2 中的行号 | 该行的完整内容 | ✓/✗ |
|---|----------|-----------------|----------------|-----|
| 1 | `</role>` | 行 X | "X: </role>" | |
| 2 | `</goal>` | 行 X | "X: </goal>" | |
| 3 | `</input>` | 行 X | "X: </input>" | |
| 4 | `</output_format>` | 行 X | "X: </output_format>" | |
| 5 | `</constraints>` | 行 X | "X: </constraints>" | |
| 6 | `</error_handling>` | 行 X | "X: </error_handling>" | |
| 7 | `</examples>` | 行 X | "X: </examples>" | |

### "该行的完整内容" 列

必须从 Step 2 复制该行的完整内容。

例如：
- ✅ 正确："005: </role>"
- ❌ 错误："005: " （空的）
- ❌ 错误：只写行号不写内容

### ``` 配对检查

从 Step 2 中找出所有包含 ``` 的行：

| 行号 | 该行完整内容 |
|------|--------------|
| X | "X: ```python" |
| Y | "Y: ```" |

总数必须是偶数。

### 空表格行检查

在 Step 2 中搜索包含 `|  |` 或 `| |` 的行：

- 未找到 → ✓
- 找到 → 列出行号和内容

---

## Step 4: 一致性校验

### 字符数校验

Step 1 提示词字符数：___
Step 2 编号内容字符数（不含行号）：___

两者必须相等（误差 < 5%）。

### 标签数校验

Step 1 中 `<` 出现次数：___
Step 2 中 `<` 出现次数：___

两者必须相等。

---

## Step 5: 结果

如果 Step 3 全部 ✓ 且 Step 4 一致 → "通过 ✅"
否则 → 说明问题，回到 Step 1 重新生成

---

## 禁止行为

⛔ 禁止 Step 2 的行显示为空但 Step 3 声称找到内容
⛔ 禁止 Step 2 省略 XML 标签
⛔ 禁止 Step 1 和 Step 2 字符数差距 > 5%
⛔ 禁止 Step 3 的 "该行完整内容" 列与 Step 2 不一致

---

## 示例

### Step 1 输出示例
```xml
<role>
你是专家。
</role>

<goal>
完成任务。
</goal>
```

### Step 2 编号示例（正确）
```
001: <role>
002: 你是专家。
003: </role>
004: 
005: <goal>
006: 完成任务。
007: </goal>
```

### Step 2 编号示例（错误 - 省略标签）
```
001: 
002: 你是专家。
003: 
004: 
005: 
006: 完成任务。
007: 
```
这是错误的！标签被省略了。

---

现在开始执行 Step 1。
```
