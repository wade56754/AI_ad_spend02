# Skill 强制修复提示词

> 复制以下全部内容发送给 Claude

---

```
# 任务：修复 prompt-optimizer skill

你需要修复这个 skill，使其生成的提示词符合规范。

## 执行流程

⛔ 严格按顺序执行，禁止跳步

### Step 1: 生成测试提示词

用当前 skill 为需求"编写 API 测试用例"生成一个提示词。
完整输出，从 <role> 到最后一个闭合标签。

### Step 2: 逐项验证（展示证据）

对生成的提示词执行以下检查，**必须展示搜索结果作为证据**：

| # | 检查项 | 验证方法 | 你的搜索结果 | ✓/✗ |
|---|--------|----------|--------------|-----|
| 1 | ``` 成对 | 数 ``` 出现次数，必须是偶数 | "找到 X 个" | |
| 2 | <role> 闭合 | 搜索 </role> | "第 X 行找到" 或 "未找到" | |
| 3 | <goal> 闭合 | 搜索 </goal> | "第 X 行找到" 或 "未找到" | |
| 4 | <input> 闭合 | 搜索 </input> | "第 X 行找到" 或 "未找到" | |
| 5 | <output_format> 闭合 | 搜索 </output_format> | "第 X 行找到" 或 "未找到" | |
| 6 | <constraints> 存在且闭合 | 搜索开始和结束标签 | "第 X 行找到" 或 "未找到" | |
| 7 | <error_handling> 存在且闭合 | 搜索开始和结束标签 | "第 X 行找到" 或 "未找到" | |
| 8 | <examples> 存在且闭合 | 搜索开始和结束标签 | "第 X 行找到" 或 "未找到" | |
| 9 | 无空表格行 | 搜索 "| |" 或 "|  |" | "未找到" 或 "第 X 行找到" | |

⚠️ "你的搜索结果" 列必须填写具体内容，不能为空
⚠️ 如果写 ✓ 但搜索结果是"未找到"，视为作弊

### Step 3: 列出所有失败项

把 Step 2 中 ✗ 的项目列出来：

```
失败项：
1. [检查项名称]: [问题描述]
2. ...
```

如果没有失败项，跳到 Step 6。

### Step 4: 分析根因

对每个失败项，分析是 skill 的哪个文件导致的：

| 失败项 | 根因分析 | 需修复的文件 |
|--------|----------|--------------|
| 缺少 <constraints> | signature.md 模板不完整 | rules/signature.md |

### Step 5: 输出修复代码

对每个需要修复的文件，输出**完整的修复后内容**（不是 diff）：

```markdown
## 文件: rules/signature.md

{完整的修复后文件内容}
```

### Step 6: 生成修复后的提示词

应用修复后，重新生成"编写 API 测试用例"的提示词。
完整输出，从 <role> 到最后一个闭合标签。

### Step 7: 再次验证（必须全部通过）

对 Step 6 的提示词重复 Step 2 的检查：

| # | 检查项 | 搜索结果 | ✓/✗ |
|---|--------|----------|-----|
| 1 | ``` 成对 | | |
| 2 | </role> | | |
| 3 | </goal> | | |
| 4 | </input> | | |
| 5 | </output_format> | | |
| 6 | <constraints> 存在且闭合 | | |
| 7 | <error_handling> 存在且闭合 | | |
| 8 | <examples> 存在且闭合 | | |
| 9 | 无 "\| \|" | | |

如果仍有 ✗，返回 Step 5 继续修复，直到全部 ✓。

---

## Skill 文件内容

### SKILL.md
{粘贴 SKILL.md 内容}

### rules/intent.md
{粘贴内容}

### rules/signature.md
{粘贴内容}

### rules/quality.md
{粘贴内容}

### rules/claude.md
{粘贴内容}

---

## 参考模板

修复后的提示词必须符合以下结构：

```xml
<role>
{角色定义}
</role>

<goal>
{任务目标}
</goal>

<input>
{输入说明}
</input>

<output_format>
{输出模板}

```python
# 代码示例
```

{其他输出内容}
</output_format>

<constraints>
1. {约束1}
2. {约束2}
3. {约束3}
</constraints>

<error_handling>
- 如果 {情况1}: {处理}
- 如果 {情况2}: {处理}
- 如果 {情况3}: {处理}
</error_handling>

<examples>
输入：
{示例输入}

输出：
{示例输出}
</examples>
```

---

## 禁止行为

⛔ 禁止在验证表格中写 ✓ 但搜索结果为"未找到"
⛔ 禁止跳过任何 Step
⛔ 禁止在 Step 7 仍有 ✗ 时就结束
⛔ 禁止只输出 diff，必须输出完整文件内容
⛔ 禁止把 <examples> 内容混在 <output_format> 里

---

现在开始执行 Step 1。
```
