# AI广告代投系统 - 快速启动指南

## 🚀 开发环境现状

✅ **后端服务已启动**
- 地址: http://localhost:8001
- API文档: http://localhost:8001/docs
- 状态: ✅ 运行中

✅ **前端服务已启动**
- 地址: http://localhost:3002
- 状态: ✅ 运行中

## 📋 系统架构

### 后端 (Claude)
- **框架**: FastAPI + Python 3.11
- **端口**: 8001
- **API模式**: 简化版 (模拟数据)
- **文档**: Swagger UI

### 前端 (Bolt.new)
- **框架**: Next.js 15 + TypeScript
- **端口**: 3002
- **UI库**: shadcn/ui
- **状态**: 基础框架运行中

## 🎯 可用功能

### ✅ 已实现的模块
1. **项目管理** - 完整的CRUD和可视化
2. **财务管理** - 充值申请和审批
3. **广告账户管理** - 账户状态管理
4. **日报系统** - 数据提交和审核
5. **对账系统** - 财务对账功能

### 🔧 核心API端点
```bash
# 健康检查
curl http://localhost:8001/health

# 项目列表
curl http://localhost:8001/api/v1/projects

# 项目详情
curl http://localhost:8001/api/v1/projects/1
```

## 🛠️ 开发工具

### 启动服务
```bash
# Windows (推荐)
.\scripts\start-dev.bat

# Linux/Mac
./scripts/start-dev.sh
```

### 停止服务
```bash
# Windows
.\scripts\stop-dev.bat

# Linux/Mac
./scripts/stop-dev.sh
```

## 📂 项目结构

```
AI_ad_spend02/
├── backend/                 # Claude后端代码
│   ├── simple_backend.py   # 简化版后端服务
│   └── init.sql           # 数据库初始化脚本
├── frontend/               # Bolt.new前端代码
│   ├── app/               # Next.js页面
│   ├── components/        # UI组件
│   └── .env.local         # 环境变量
├── scripts/               # 开发工具脚本
│   ├── start-dev.bat      # Windows启动脚本
│   └── stop-dev.bat       # Windows停止脚本
└── docs/                  # 开发文档
```

## 🔄 开发流程

### 1. Claude后端开发
```python
# 在Claude中请求API开发
"""
请为AI广告代投系统创建用户管理API，包括：
- GET /api/v1/users - 用户列表
- POST /api/v1/users - 创建用户
- PUT /api/v1/users/{id} - 更新用户
- DELETE /api/v1/users/{id} - 删除用户

要求统一响应格式和错误处理
"""
```

### 2. Bolt.new前端开发
```typescript
// 在Bolt.new中请求组件开发
"""
基于以下API创建用户管理界面：
- GET /api/v1/users
- POST /api/v1/users
- PUT /api/v1/users/{id}
- DELETE /api/v1/users/{id}

要求使用shadcn/ui组件，包含CRUD操作
"""
```

### 3. 实时协调
- Claude生成API → 更新OpenAPI文档
- Bolt.new读取API → 生成TypeScript类型
- 双向同步 → WebSocket实时通信

## 📊 当前数据

### 项目数据
```json
{
  "id": 1,
  "name": "春季推广活动",
  "client_name": "ABC科技公司",
  "budget": 50000,
  "current_spend": 32500,
  "status": "active"
}
```

### 系统状态
- 数据库: SQLite模拟
- 缓存: 无
- 认证: 简化版
- 实时: 基础WebSocket

## 🎨 下一步开发

### 立即可做
1. **访问前端**: http://localhost:3002
2. **测试API**: http://localhost:8001/docs
3. **查看项目列表**: /projects
4. **创建新项目**: 项目管理界面

### 协作开发
1. **Claude专注后端**: API、数据库、业务逻辑
2. **Bolt.new专注前端**: UI、交互、用户体验
3. **实时同步**: WebSocket + React Query

## 🔗 相关文档

- [Claude与Bolt.new协调开发指南](docs/development/claude-bolt-coordination-guide.md)
- [具体操作手册](docs/development/claude-bolt-operations-manual.md)
- [API设计规范](docs/development/api-design-for-frontend.md)

## ⚠️ 注意事项

1. **端口冲突**: 如果端口被占用，脚本会自动处理
2. **依赖安装**: 首次运行会自动安装必要依赖
3. **浏览器缓存**: 前端更新可能需要强制刷新
4. **API版本**: 确保前后端API版本匹配

---

**🎉 现在可以开始开发了！**

访问 http://localhost:3002 开始使用前端应用
访问 http://localhost:8001/docs 查看后端API文档