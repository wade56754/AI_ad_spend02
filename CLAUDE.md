# AI广告代投系统 - 项目导航指南

> **项目名称**: AI广告代投系统
> **版本**: v2.2
> **更新日期**: 2025-11-14
> **文档类型**: 项目入口和导航中心

本指南为AI广告代投系统提供完整的项目概览、文档导航和快速启动指南，帮助不同角色的团队成员快速了解和参与项目。

---

## 🎯 项目概览

### 项目简介
AI广告代投系统是一个智能化的广告投放管理平台，专为Facebook广告代理商设计，通过AI技术提升投放效率、降低成本、优化ROI。

### 核心价值
- **效率提升**: 自动化日报处理，节省90%人工时间
- **成本控制**: 精准的对账系统，避免财务损失
- **智能决策**: AI驱动的账户寿命预测和异常检测
- **风险管控**: 实时监控和预警，及时发现问题

### 技术亮点
- **现代化架构**: FastAPI + Next.js + PostgreSQL
- **安全可靠**: RLS权限控制 + JWT认证 + 审计日志
- **高性能**: Redis缓存 + 数据库优化 + 异步处理
- **可扩展**: 微服务架构 + Docker容器化 + CI/CD

### 目标用户
- **广告代理商**: 多项目管理、客户关系维护
- **投手**: 日常广告投放、数据录入、报表提交
- **户管**: 账户分配、日报审核、数据管理
- **财务**: 充值审批、对账管理、财务分析
- **管理者**: 整体监控、ROI分析、团队管理

### 核心功能
- 📊 **智能项目管理**: 项目生命周期管理，ROI分析
- 👥 **多角色协作**: 投手、户管、财务、管理员协同工作
- 🔒 **安全权限控制**: 基于RLS的数据隔离和权限管理
- 🤖 **AI智能监控**: 账户异常检测、性能预警、寿命预测
- 💰 **自动对账系统**: 精准的财务对账和差异分析
- 📈 **实时数据监控**: 消耗、转化、ROI等关键指标监控

---

## 📚 文档导航体系

### 🏗️ 核心文档索引

#### 主技术文档
- **[AI广告代投系统开发文档_优化版v2.1.md](./AI广告代投系统开发文档_优化版v2.1.md)** - 完整的技术架构和开发规范

#### 系统架构文档
- **[SYSTEM_OVERVIEW.md](./docs/core/SYSTEM_OVERVIEW.md)** - 系统架构概览、业务模式、权限矩阵
- **[DATA_SCHEMA.md](./docs/core/DATA_SCHEMA.md)** - 数据库设计、表结构定义（唯一真相源）
- **[API_DEVELOPMENT_FLOW.md](./docs/core/API_DEVELOPMENT_FLOW.md)** - API开发流程规范（强制执行）
- **[STATE_MACHINE.md](./STATE_MACHINE.md)** - 状态机定义、业务流程、权限控制

### 💻 开发文档
- **[BACKEND_API_GUIDE.md](./BACKEND_API_GUIDE.md)** - 后端API开发指南、接口规范
- **[FRONTEND_GUIDE.md](./FRONTEND_GUIDE.md)** - 前端开发指南、组件库使用
- **[DEVELOPMENT_ENVIRONMENT_SETUP.md](./DEVELOPMENT_ENVIRONMENT_SETUP.md)** - 开发环境配置、工具链

### 🔧 运维文档
- **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** - 部署指南、Docker配置、CI/CD
- **[MONITORING_OPS.md](./MONITORING_OPS.md)** - 监控运维、告警系统、故障处理
- **[SECURITY_CONFIG.md](./SECURITY_CONFIG.md)** - 安全配置、权限策略、漏洞管理
- **[TESTING_STRATEGY.md](./TESTING_STRATEGY.md)** - 测试策略、质量保证、自动化

### 📋 角色阅读路径

#### 👥 项目管理者
**推荐阅读路径**:
1. [项目概览](#-项目概览) → 了解项目价值和技术亮点
2. [SYSTEM_OVERVIEW.md](./SYSTEM_OVERVIEW.md) → 掌握业务模式和权限体系
3. [开发阶段规划](#-项目管理指南) → 了解开发进度和里程碑
4. [验收标准](#-验收标准) → 确认交付质量

**重点关注**: 业务价值、开发进度、验收标准、ROI指标

#### 💻 后端开发者
**推荐阅读路径**:
1. [开发环境配置](#-快速开始) → 搭建开发环境
2. **[API_DEVELOPMENT_FLOW.md](./docs/core/API_DEVELOPMENT_FLOW.md) → API开发流程（强制规范）**
3. [DATA_SCHEMA.md](./docs/core/DATA_SCHEMA.md) → 数据库设计（唯一真相源）
4. [BACKEND_API_GUIDE.md](./BACKEND_API_GUIDE.md) → API设计规范
5. [STATE_MACHINE.md](./STATE_MACHINE.md) → 状态机实现和业务逻辑
6. [主技术文档](./AI广告代投系统开发文档_优化版v2.1.md) → 完整技术规范

**重点关注**: API规范、数据库设计、状态机、安全配置

#### 🎨 前端开发者
**推荐阅读路径**:
1. [开发环境配置](#-快速开始) → 搭建前端开发环境
2. [FRONTEND_GUIDE.md](./FRONTEND_GUIDE.md) → 前端开发规范和组件使用
3. [BACKEND_API_GUIDE.md](./BACKEND_API_GUIDE.md) → API接口规范和调用方式
4. [主技术文档](./AI广告代投系统开发文档_优化版v2.1.md) → 系统架构和接口说明

**重点关注**: 组件库使用、API调用、状态管理、权限控制

#### 🔧 运维工程师
**推荐阅读路径**:
1. [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) → 部署配置和环境管理
2. [MONITORING_OPS.md](./MONITORING_OPS.md) → 监控系统和告警配置
3. [SECURITY_CONFIG.md](./SECURITY_CONFIG.md) → 安全配置和漏洞管理
4. [TESTING_STRATEGY.md](./TESTING_STRATEGY.md) → 测试策略和质量保证

**重点关注**: 部署配置、监控告警、安全防护、备份恢复

---

## 🚀 快速开始

### 系统要求
- **Node.js**: 18.0+
- **Python**: 3.11+
- **Docker**: 4.0+
- **Git**: 2.30+

### 一键启动
```bash
# 克隆项目
git clone <repository-url>
cd ai-ad-spend

# 环境检查
./scripts/check-environment.sh

# 启动开发环境
docker-compose -f docker-compose.dev.yml up -d

# 初始化数据库
./scripts/init-database.sh

# 启动前端
cd frontend && npm run dev

# 启动后端
cd backend && uvicorn app.main:app --reload
```

### 访问地址
- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs
- **Grafana监控**: http://localhost:3001

### 开发环境配置
```bash
# 安装前端依赖
cd frontend && npm install

# 安装后端依赖
cd backend && pip install -r requirements.txt

# 配置环境变量
cp .env.example .env.local
# 编辑 .env.local 添加必要配置

# 数据库迁移
cd backend && alembic upgrade head
```

### 常用开发命令
```bash
# 前端开发
npm run dev          # 开发服务器
npm run build        # 生产构建
npm run test         # 运行测试
npm run lint         # 代码检查

# 后端开发
uvicorn app.main:app --reload  # 开发服务器
pytest -v --disable-warnings     # 运行测试
black app/                    # 代码格式化
isort app/                    # 导入排序

# 数据库操作
alembic upgrade head    # 执行迁移
alembic revision --autogenerate -m "migration message"  # 创建迁移

# Docker操作
docker-compose -f docker-compose.dev.yml up -d    # 启动开发环境
docker-compose -f docker-compose.dev.yml logs -f  # 查看日志
docker-compose -f docker-compose.dev.yml down   # 停止服务
```

---

## 💻 开发者指南

### 技术栈
- **后端**: FastAPI + Pydantic v2 + SQLAlchemy (同步版)
- **前端**: Next.js 16.0.2 + TypeScript + Tailwind + shadcn/ui
- **数据库**: PostgreSQL (Supabase托管)
- **缓存**: Redis + RQ
- **监控**: Prometheus + Grafana + Sentry

### 开发规范
- **代码格式化**: Prettier + ESLint (前端), Black + isort (后端)
- **提交规范**: Conventional Commits
- **API规范**: 统一返回格式 + 错误码
- **测试要求**: 单元测试 + 集成测试 + E2E测试

### 核心开发流程
1. **创建功能分支** → 编写代码 → 提交PR
2. **代码审查** → 自动化测试 → 部署到测试环境
3. **QA测试** → 验收 → 合并到主分支
4. **自动化部署** → 监控确认 → 功能上线

### 分支管理
- `main`: 主分支，生产环境代码
- `develop`: 开发分支，集成测试
- `feature/*`: 功能分支，新功能开发
- `hotfix/*`: 热修复分支，紧急修复

### API开发标准
```python
# 统一响应格式
{
  "success": true,
  "data": {...},
  "message": "操作成功",
  "code": "SUCCESS",
  "request_id": "uuid",
  "timestamp": "2025-11-11T10:30:00Z"
}

# 错误响应
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "参数验证失败"
  },
  "request_id": "uuid",
  "timestamp": "2025-11-11T10:30:00Z"
}
```

---

## 🔧 运维管理

### 部署环境
- **开发环境**: 本地开发，热重载
- **测试环境**: 自动部署，数据脱敏
- **预生产环境**: 生产数据镜像，完整测试
- **生产环境**: 蓝绿部署，零停机

### 监控指标
- **应用指标**: 响应时间、错误率、吞吐量
- **业务指标**: 消耗、转化、ROI、账户状态
- **基础设施**: CPU、内存、磁盘、网络
- **安全指标**: 登录失败、异常访问、攻击检测

### 备份策略
- **数据库**: 每日全量备份 + 实时增量备份
- **配置文件**: 版本控制管理
- **日志文件**: 集中收集和长期存储
- **代码**: Git仓库 + 多地备份

### 故障处理
1. **监控告警** → 自动通知相关人员
2. **快速诊断** → 定位问题根因
3. **紧急修复** → 实施临时解决方案
4. **根本解决** -> 彻底修复问题并预防

---

## 👥 项目管理指南

### 项目状态
- **当前版本**: v2.1 (2025-11-11)
- **开发阶段**: P0核心功能开发中
- **预计上线**: 3个月内

### 核心业务流程
```
甲方签约付款 → 创建项目 → 申请渠道账户 → 分配账户给投手
      ↓
投手投放广告 → 每日提交日报 → 数据员审核 → 确认粉数
      ↓
提交充值申请 → 数据员审核需求 → 财务审批 → 财务打款
      ↓
代理商充值 → 月底财务对账 → 项目盈利分析 → AI优化建议
```

### 开发阶段规划

#### P0阶段：核心业务功能（3-4周）
- ✅ 用户认证和权限系统
- ✅ 项目管理和渠道管理
- ✅ 广告账户管理
- ✅ 日报提交流程
- ✅ 充值申请流程

#### P1阶段：财务和数据（2-3周）
- 🔄 财务对账系统
- 🔄 成本分析模块
- 🔄 数据导入工具
- 🔄 基础报表功能

#### P2阶段：AI和自动化（2-3周）
- ⏳ AI异常检测
- ⏳ 账户寿命预测
- ⏳ 自动化通知
- ⏳ 高级报表功能

### 验收标准
- ✅ **核心流程完整**：日报、充值、对账端到端可跑通
- ✅ **安全控制严格**：所有操作都有权限验证和审计日志
- ✅ **API响应统一**：所有接口都遵循统一的响应格式
- ✅ **监控覆盖全面**：关键业务指标都有监控和告警
- ✅ **部署自动化**：支持一键部署和回滚

### 团队协作
- **架构团队**: 技术架构、核心模块、代码审查
- **开发团队**: 前端、后端、数据库、AI模块
- **测试团队**: 功能测试、性能测试、安全测试
- **运维团队**: 部署、监控、备份、故障处理
- **产品团队**: 需求分析、用户体验、项目协调

---

## 📞 技术支持

### 文档反馈
- **技术问题**: 提交GitHub Issue
- **文档更新**: 联系架构团队
- **紧急故障**: 联系运维负责人

### 联系方式
- **技术架构师**: architect@company.com
- **开发团队负责人**: dev-team@company.com
- **运维负责人**: ops-team@company.com
- **产品负责人**: product@company.com

### 相关资源
- **代码仓库**: https://github.com/your-org/ai-ad-spend
- **项目管理**: https://project.yourdomain.com
- **监控面板**: https://monitor.yourdomain.com
- **文档中心**: https://docs.yourdomain.com

---

## 🤖 AI助手开发规范

### 项目规则记忆
为了确保代码的一致性和质量，AI助手必须遵循存储在 [`.project-rules.md`](./.project-rules.md) 中的所有项目规范。

### 核心开发原则

1. **严格遵循技术栈规范**
   - 后端：FastAPI + Pydantic v2 + SQLAlchemy (同步版)
   - 前端：Next.js 16.0.2 + TypeScript + Tailwind + shadcn/ui
   - 数据库：PostgreSQL + RLS权限控制
   - 缓存：Redis + RQ异步队列

2. **代码规范要求**
   - Python必须使用类型注解和文档字符串
   - TypeScript必须有严格的类型定义
   - 遵循统一的代码格式化（Black/isort/Prettier）
   - 所有API必须有适当的错误处理和日志

3. **安全要求**
   - 所有API端点都需要JWT认证
   - 实现基于角色的权限控制（5个角色）
   - 敏感数据必须加密存储
   - 遵循RLS数据隔离原则

4. **API响应格式**
   ```python
   # 成功响应（必须格式）
   {
     "success": true,
     "data": {...},
     "message": "操作成功",
     "code": "SUCCESS",
     "request_id": "uuid",
     "timestamp": "2025-11-11T10:30:00Z"
   }
   ```

5. **数据库设计原则**
   - 表名使用复数形式（users, projects）
   - 必须包含created_at和updated_at字段
   - 外键必须创建索引
   - 大表必须考虑分区

6. **测试要求**
   - 单元测试覆盖率 > 80%
   - 所有API必须有集成测试
   - 关键业务流程需要E2E测试

7. **Git提交规范**
   ```
   <type>(<scope>): <subject>

   type: feat, fix, docs, style, refactor, perf, test, chore
   ```

### 开发检查清单
在编写或修改代码前，AI助手必须：
- [ ] 查阅 [`.project-rules.md`](./.project-rules.md) 确认规范
- [ ] 检查是否遵循统一的命名规范
- [ ] 确认是否包含适当的错误处理
- [ ] 验证是否实现了必要的权限控制
- [ ] 检查是否添加了必要的日志记录

### 重要提醒
- **禁止**硬编码敏感信息（使用环境变量）
- **必须**使用依赖注入而非直接实例化
- **必须**处理所有可能的异常情况
- **禁止**在生产环境输出调试信息
- **必须**遵循项目的目录结构和文件命名规范

### 常见问题
1. **Q**: 如何处理权限验证？
   **A**: 使用 `require_role` 装饰器，传入允许的角色列表

2. **Q**: 如何实现分页？
   **A**: 使用 `page` 和 `size` 参数，返回 `PaginatedResponse`

3. **Q**: 如何处理数据库事务？
   **A**: 使用 `db.begin()` 上下文管理器

4. **Q**: 如何添加审计日志？
   **A**: 使用 `audit_log` 服务记录关键操作

### 🤖 Claude 专项约束

针对 Claude 先前出现的“全面清理/误删/语法拼写遍历”等问题，补充以下强制约束：

1. **任务限定**
   - 所有请求需指向明确文件或模块，禁止“全仓修复拼写/检查所有语法错误”。
   - 执行前必须输出操作计划与文件列表并等待确认；若用户未确认，只能继续分析，不得修改。

2. **修改前检查**
   - 先查看 `git status`/`git diff`，不得覆盖已有未提交更改。
   - 未经授权禁止删除、重建或批量移动根目录及关键文件（`frontend/app/page.tsx`、`frontend/components/*`、`backend/core/*` 等）。

3. **开发过程**
   - 语法、拼写、类型问题必须依赖 ESLint、TypeScript、pytest 等工具发现；AI 不得逐文件“肉眼修复”。
   - 改动涉及重命名/路径调整时，需使用 `rg` 等工具全局替换并（如有需要）添加兼容层。
   - UI/可访问性任务必须引用 `docs/UI_DESIGN_GUIDE.md` 对应章节，说明遵循的规范和验收方式。

4. **验证与输出**
   - 后端改动需跑 `pytest`（或相应标记），前端改动需跑 `pnpm lint`/`pnpm test`，并在回复中写明结果；无法执行须说明原因与替代验证。
   - 最终回复按“修改内容 + 影响文件 + 验证方式”格式输出，禁止笼统描述（如“全面优化”“重建组件系统”）。

5. **安全与异常**
   - 禁止在配置、日志中打印真实密钥或数据库 URL，调试信息必须受 `debug`/日志等级控制。
   - 若发现与当前任务无关的异常（测试失败、脏工作区等），必须暂停并询问用户下一步，不得自作主张清理。

#### 典型错误防范清单
为避免近期重复出现的严重问题，Claude 在收到相关任务时需逐条自检：

1. **React 组件渲染**
   - 不得直接渲染 React 组件对象；在 `module.icon` 一类字段上必须使用 `React.createElement` 或 JSX。
   - 引用的图标/组件必须在文件顶部显式导入，CI 中若报 “Objects are not valid as a React child” 需立即回查渲染逻辑。

2. **导入与模块路径**
   - 修改/新增模块后，立刻搜索整个仓库更新 import；禁止通过 `sed` 等命令批量替换而不验证路径存在。
   - 如果只存在 `core/db.py`，不得再引用 `core.database`，必要时新增 shim 并记录清退计划。

3. **ORM/加密配置**
   - SQLAlchemy 基础对象必须保证 `declarative_base`、`sessionmaker` 等依赖已导入；缺失引用要补齐并加单测。
   - `bcrypt` 相关代码一律操作 bytes，不允许对 `gensalt()` 结果再次 `.encode()`；发现 AttributeError 时禁止忽略。

4. **配置与日志**
   - 数据库 URL、JWT、加密 key 等只可在受控日志等级下输出，并需脱敏（例如保留前 4 位与长度）。
   - 新增配置字段（如连接池超时）必须同步更新 `Settings`、`.env.example` 以及文档，否则视为违规。

5. **前端交互**
   - Header/Sidebar 等交互组件必须提供可触达的 UI 逻辑（点击/ESC/点击外部关闭），不得留下 `hidden` 永久不可见的 DOM。
   - 响应式、深浅色主题、可访问性调整需引用 `docs/UI_DESIGN_GUIDE.md` 对应章节，并说明调试手段（截图、键盘导航、axe 等）。

6. **Bug 分类与验证**
   - 针对用户标记为 Critical/Major 的问题，必须在描述中体现根因与防线（测试、lint、设计规范），否则不接受。
   - 任何“批量清理/统一优化”类操作需要列出具体文件、具体问题与验证结果，严禁含糊其辞地声称“已解决 X 个 Bug”。

#### 自查与回报流程
所有代码改动任务必须遵循以下步骤，未获确认不得跳过：

1. **自查计划**
   - 列出本次要检查的文件/模块及潜在风险点（导入、配置、UI、测试等）。
   - 明确将使用的工具与手段（`rg`、`tsc`、`pnpm lint`、`pytest`、截图/Storybook 等）。
   - 计划需先提交并等待确认，确认前只能分析，禁止修改代码。

2. **执行检查**
   - 根据计划逐项运行工具，形成“问题清单”（包含报错/警告/需复核项）。
   - 每条清单都必须说明处理方案或无需处理的理由，不得省略。
   - 若结论为“需要全面整改”，须先提交“清单 + 处理计划”并获批。

3. **修复与验证**
   - 按确认过的计划逐条修复，并在清单中标注“已修复 + 验证方式（命令/截图/复测步骤）”。
   - 修改后重新运行对应的 lint/test 并提供关键输出；无法运行时说明原因及替代验证。

4. **最终汇报**
   - 用“文件/模块 + 具体改动 + 验证结果”格式总结，逐条勾选初始清单，确认无遗漏。
   - 如仍存在未解决项或新增风险，必须在总结中指出并请求下一步指示。

---

**文档版本**: v2.2
**最后更新**: 2025-11-14
**下次审查**: 功能重大变更时
**维护责任人**: 项目架构团队
- # AI 模块开发规则（AI_MODULE_RULES.md）

> 适用对象：Claude Code / ChatGPT / 其他 AI 编程助手  
> 适用范围：AI_ad_spend / AI 财务与投手管理系统 的后端、前端、脚本开发与维护

---

## 1. 总体目标

1. 在 **不破坏现有架构** 的前提下，完成指定范围内的功能开发或修改。  
2. 优先 **对齐现有代码风格与模块模式**，而不是引入新的框架和结构。  
3. 保证每次改动都是 **可解释、可回滚、可测试** 的最小变更，而不是“大手一挥重构一片”。  

---

## 2. 任务范围控制

AI 必须严格遵守以下边界：

1. **只处理用户明确指定的范围**：  
   - 指定文件（例如：`backend/routes/topups.py`）  
   - 指定模块（例如：“充值申请 API 模块”）  
   - 指定问题（例如：“修复充值列表分页错误”）  
2. **禁止以下行为（除非用户明确要求）**：  
   - 全局格式化、重排、重命名所有文件  
   - 重写现有模块的整体架构（路由结构、响应封装方式、异常体系等）  
   - 擅自修改数据库结构 / 迁移脚本（特别是生产环境相关）  
   - 大规模“搜索替换”类改动（跨目录、多模块）  
3. 如确实认为需要做大范围重构，必须：  
   - 先提出理由 & 风险分析  
   - 给出备选方案（最小修改 vs 大改）  
   - 等用户明确同意后再执行。  

---

## 3. 修改前：信息收集与对齐

在对代码做任何修改之前，必须遵循以下步骤：

1. **理解需求与上下文**  
   - 复述：用自己的话简要说明这次任务的目标和范围。  
   - 确认：有哪些业务前提？哪些属于未来扩展，不在本次范围内？  

2. **查找相关文件与参考模块**  
   - 按照用户给出的路径，打开对应文件。  
   - 如果是新增模块，必须先查找 **同类模块** 作为参考，例如：  
     - 新增财务类模块 → 先看 `projects` / `ad_accounts` / `topups` 的现有实现  
     - 新增前端页面 → 先看已有的 Dashboard / 列表页面 / 详情页  

3. **输出修改计划（必须）**  
   在真正改代码前，先给出一个简短的“行动计划”，例如：

   ```text
   本次改动计划：
   1）阅读并理解当前模块现有逻辑（文件 A/B/C）
   2）在 routes 中增加一个新的 GET /topups/{id} 接口
   3）在 service 层新增对应的查询方法，复用现有 repository
   4）增加 2～3 个 pytest 用例覆盖正常和异常场景
   5）运行 pytest 并汇报结果
   ```

   等用户确认后再动手。  

---

## 4. 修改中：对齐现有模式，而不是另起炉灶

### 4.1 代码结构与分层

在后端（FastAPI + Supabase）中：

1. **优先复用现有分层模式**：  
   - 如果项目已有 `routers/`, `services/`, `schemas/` 等目录：  
     - 新接口 → 加在对应 router 文件  
     - 新业务逻辑 → 对应 service / repository  
     - 入参 / 出参 → 对应 Pydantic schema  
2. 不要：  
   - 自己设计一套新的“通用响应封装层”  
   - 引入新的依赖注入框架 / 装饰器体系  
   - 把简单逻辑硬拆成大量过度设计的层次  

在前端（Next.js + Tailwind + shadcn/ui）中：

1. 新页面必须遵守：  
   - 文件结构与已有页面一致（例如使用 `app/(dashboard)/xxx/page.tsx`）  
   - UI 组件优先复用现有的 `components/ui`、现有 layout 组件  
2. 样式必须基于：  
   - Tailwind + 设计 token（不要硬写魔法数 CSS）  
   - 尊重 `UI_DESIGN_GUIDE_v1.2` 中的组件与布局规范（如统计卡、表格、图表容器等）  

---

### 4.2 命名与风格

1. 函数、变量、类型命名必须与现有代码风格一致：  
   - 后端：`snake_case` 函数与变量，`PascalCase` 类名、Schema 对象  
   - 前端：`camelCase` 变量与函数，`PascalCase` 组件名  
2. 严禁引入与现有项目风格完全不同的命名方式（例如在同一项目中混用 `reqBody` 和 `request_payload`）。  

---

### 4.3 业务逻辑与边界处理

在实现/修改逻辑时必须检查：

1. 正常流程是否完整？  
2. 边界情况是否覆盖？例如：  
   - 空列表 / NULL 值 / 未找到记录  
   - 传参缺失或非法  
   - 重复请求、幂等性要求  
3. 是否符合项目现有业务规则：  
   - 例如充值状态流转：`pending -> approved/rejected`，不得随意跳状态。  
   - 预算、余额、成本相关计算必须注意精度与单位。  

---

## 5. 修改后：检查、运行与汇报

改完代码后，AI 必须按照以下顺序进行自检：

### 5.1 静态检查（如项目支持）

优先尝试：

- Python 后端：  
  - `pytest` 或 `pytest path/to/tests`  
  - （如项目已有）类型检查器 / linter（例如 `mypy` / `ruff` 等）  
- 前端：  
  - `pnpm lint` / `pnpm test` / `pnpm build`（取决于项目已有脚本）  

> 重要：**不能擅自添加新的依赖或在项目根目录创建新的工具配置文件**  
> （例如随意加 `mypy.ini`、`pyproject.toml`、`eslint.config.js`），除非用户明确要求。  

### 5.2 真实执行情况必须如实报告

AI 在响应中必须清楚写明：

1. 实际运行了哪些命令（完整命令）  
2. 对于每个命令：  
   - 是否执行成功？  
   - 如果失败：错误信息重点内容、可能的原因分析  
3. 如果由于环境限制不能执行命令：  
   - 必须显式说明“未实际运行测试，仅做了静态代码检查”，**不得伪造测试通过的结论**。  

示例（正确汇报方式）：

```text
已尝试运行：pytest tests/test_topups.py
结果：命令执行失败，错误为 ImportError: No module named 'backend.config'

原因初步分析：测试环境缺少 backend 包的可导入路径配置（可能需要 editable 安装或调整 PYTHONPATH）。
当前仅做了静态检查，未完成自动化测试执行。
```

---

## 6. 与用户的协作约定

1. 每次操作前给出计划，等待简短确认后再执行。  
2. 当发现：  
   - 需求不清楚  
   - 与现有逻辑冲突  
   - 涉及高风险改动（数据库、权限、批量脚本）  
   时，必须暂停执行，向用户说明风险与选项。  
3. 对同一个问题，不要在未确认的情况下连续尝试多种“完全不同的方案”，避免把项目状态搞乱。  

---

## 7. 安全与配置相关限制

1. 不得在代码中写入真实敏感信息：  
   - 数据库 URL  
   - API Key  
   - Access Token / Refresh Token  
2. 所有密钥、连接串只能使用占位符，例如：  
   - `<SUPABASE_URL>`  
   - `<SERVICE_ROLE_KEY>`  
3. 修改与部署、环境变量、CI/CD 相关的文件时（如 Dockerfile、GitHub Actions、Railway Config）：  
   - 必须先说明改动意图与潜在影响  
   - 等用户确认后再执行修改。  

---

## 8. 禁止行为清单（AI 严禁做的事）

除非用户明确指令允许，否则严禁：

1. 一次性大规模格式化/重写整个项目。  
2. 大幅调整数据库结构、删除表、修改迁移历史。  
3. 为了让本地测试通过而：  
   - 胡乱注释掉测试代码  
   - 在生产代码中添加明显“跳过检查”的逻辑（例如 try/except 全吞、强制返回成功）。  
4. 在不透明的情况下修改与安全相关的配置（CORS、认证中间件、RLS 策略等）。  

---

本规则文件的目标是：**约束 AI 的行为边界，让每次改动都“小而稳”，并尽可能贴合现有项目，而不是让 AI 自己搞一套新的工程结构。**
- 模块开发请严格按照"D:\git\1108\AI_ad_spend02\docs\rule\AI 模块开发流程规范 v2.0.md"执行开发。
从【模块需求确认】开始，先输出计划，不得跨步骤。
未得到确认不得进入下一步，也不得修改未授权模块。
- 我所有给 Claude 的复杂任务（写文档、设计 API、改代码、做分析），都希望按固定结构来处理：
1）用中文沟通；2）先在 <thinking> 里分步骤推理（读文档→对比真相→找冲突→制定计划→自检），3）再在 <answer> 里给出最终可直接使用的结果（Markdown 片段、代码、清单等），4）遇到多份文档冲突时，要严格服从我指定的“唯一真相”文件（例如 DATA_SCHEMA.md），禁止自己发明字段、角色或规则，不要擅自改动与当前任务无关的模块和文件。
- 数据库定义唯一来源：docs/core/DATA_SCHEMA.md
   - 禁止在其他文档、模型或代码中重新定义字段、表结构或关系。
   - 所有变更必须先提交并合并到 DATA_SCHEMA.md，再调整模型/迁移。

2. 前端 HTTP 调用统一使用 apiFetch (来自 lib/api.ts)
   - 禁止直接调用 fetch() 或自定义 request()。
   - 禁止在页面组件内写任何裸 fetch 请求。
   ❗模型生成的任何内容若不符合以上规范，均视为错误输出，请重新执行。
数据库字段定义必须完全遵守 docs/core/DATA_SCHEMA.md，
你刚刚生成的字段名 / 数据类型 与该文件不一致，请检查并重写。 数据库定义唯一来源：docs/core/DATA_SCHEMA.md
   - 禁止在其他文档、模型或代码中重新定义字段、表结构或关系。
   - 所有变更必须先提交并合并到 DATA_SCHEMA.md，再调整模型/迁移。