[project]
name = "ai-ad-spend-agents"
version = "0.1.0"
description = "AI 广告代投系统 - Agent CLI"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
]

[project.scripts]
ai-agents = "cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
