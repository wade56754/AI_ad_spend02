# 项目开发核心规范

> **版本**: v1.0
> **更新日期**: 2025-11-16
> **文档性质**: 最高优先级规范，所有规则冲突以此为准
> **维护责任**: 项目架构团队

---

## 📋 文档说明

### 本文档定位
- **最高优先级**：所有开发规则的总纲和仲裁标准
- **冲突解决**：当不同规范文档冲突时，以本文档为准
- **索引中心**：指向各领域详细规范文档

### 规则层次结构

```
Level 1: .project-rules.md (本文档) - 总纲和仲裁标准
    ↓
Level 2: 领域规范文档 - 各领域详细规范
    ├─ API_RULEBOOK.md (API开发总规则)
    ├─ FRONTEND_DEVELOPMENT_RULES.md (前端开发规则总纲)
    ├─ CLAUDE.md (AI助手开发规范)
    └─ AGENTS.md (协作者规范)
    ↓
Level 3: 执行指南和模板
    ├─ AI_MODULE_DEVELOPMENT_WORKFLOW.md (模块开发流程)
    ├─ UI_DESIGN_GUIDE.md (UI设计指南)
    ├─ INTERFACE_DEVELOPMENT_CHECKLIST.md (开发检查清单)
    └─ 其他模板和指南文档
    ↓
Level 4: 参考文档
    └─ 技术文档、API文档、部署指南等
```

---

## 🎯 核心技术栈（强制约束）

### 后端技术栈
- **框架**: FastAPI 0.104+
- **数据验证**: Pydantic v2 (必须使用 `ConfigDict(from_attributes=True)`)
- **ORM**: SQLAlchemy 同步版本 (禁止使用异步ORM)
- **数据库**: PostgreSQL (Supabase托管)
- **缓存**: Redis + RQ
- **认证**: JWT + Supabase Auth

### 前端技术栈
- **框架**: Next.js 16.0.2 + App Router
- **语言**: TypeScript (严格模式)
- **样式**: Tailwind CSS
- **组件库**: shadcn/ui + Radix UI
- **状态管理**: Zustand + React Hooks
- **包管理**: pnpm (禁止使用npm或yarn)

### 开发工具
- **代码格式化**: Prettier (前端) + Black (后端)
- **代码检查**: ESLint (前端) + Flake8 (后端)
- **类型检查**: TypeScript (前端) + mypy (后端)
- **测试**: Jest/Vitest (前端) + pytest (后端)
- **版本控制**: Git

---

## 🔐 用户角色权限定义（业务核心）

### 五大角色
1. **admin** - 管理员：全部权限
2. **finance** - 财务：充值审批、对账管理、财务报表
3. **data_operator** - 数据员：日报审核、账户分配、数据管理
4. **account_manager** - 户管：账户申请、状态更新、项目管理
5. **media_buyer** - 投手：日报提交、充值申请、广告投放

### 权限控制原则
- **后端**: 所有API必须验证权限 (使用 `require_role` 依赖注入)
- **前端**: UI根据角色动态显示/隐藏功能
- **数据库**: 使用RLS (Row Level Security) 实现数据隔离
- **审计**: 所有敏感操作必须记录审计日志

---

## 📐 开发规范总则

### 1. API开发规范

#### 统一响应格式（强制）
```python
# 成功响应
{
  "success": true,
  "data": {...},
  "message": "操作成功",
  "code": "SUCCESS",
  "request_id": "uuid",
  "timestamp": "2025-11-16T10:30:00Z"
}

# 错误响应
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "错误描述"
  },
  "request_id": "uuid",
  "timestamp": "2025-11-16T10:30:00Z"
}
```

#### API路径规范
- 使用RESTful风格
- 资源名称用复数形式：`/api/v1/projects` (不是 `/api/v1/project`)
- 版本前缀：`/api/v1/`
- 路径参数：`/api/v1/projects/{id}`

#### 性能要求
- P50响应时间: < 100ms
- P95响应时间: < 200ms (硬性要求)
- P99响应时间: < 500ms (硬限制)
- 超时阈值: 5秒

**详细规范**: 见 [API_RULEBOOK.md](docs/API_RULEBOOK.md)

### 2. 前端开发规范

#### 强制性约束
- ✅ **必须使用**: shadcn/ui组件库，禁止使用裸HTML标签
- ❌ **禁止使用**: `<button>`, `<input>`, `<select>` 等原生HTML元素
- ✅ **必须使用**: `<Button>`, `<Input>`, `<Select>` 等shadcn组件
- ✅ **布局组件**: 所有页面必须使用 `AppLayout` + `PageHeader`

#### TypeScript规范
- 严格模式启用
- 所有组件和函数必须有类型定义
- Props必须定义interface
- 禁止使用 `any` 类型

#### 组件命名规范
- 组件文件名: PascalCase (如 `ProjectList.tsx`)
- 组件函数: PascalCase (如 `function ProjectList()`)
- 工具函数: camelCase (如 `function formatDate()`)

**详细规范**: 见 [FRONTEND_DEVELOPMENT_RULES.md](docs/rule/FRONTEND_DEVELOPMENT_RULES.md)

### 3. 数据库设计规范

#### 表命名规范
- 使用复数形式: `users`, `projects`, `ad_accounts`
- 全小写，下划线分隔: `daily_reports`, `recharge_requests`

#### 必须字段
所有表必须包含以下审计字段:
```sql
created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
created_by UUID REFERENCES users(id),
updated_by UUID REFERENCES users(id)
```

#### 索引要求
- 外键必须创建索引
- 查询过滤字段必须创建索引
- 状态字段必须创建索引

#### RLS策略（强制）
- 所有用户表必须启用RLS
- 非admin角色只能访问自己的数据
- 使用Supabase RLS策略实现数据隔离

---

## 🔒 安全规范（零容忍）

### 敏感信息保护
- ❌ **禁止**: 在代码中硬编码密钥、密码、Token
- ❌ **禁止**: 在日志中输出密码、Token、数据库连接字符串
- ✅ **必须**: 使用环境变量存储敏感信息
- ✅ **必须**: 敏感数据脱敏后输出（如只显示前4位）

### 输入验证
- ✅ **必须**: 使用Pydantic进行后端数据验证
- ✅ **必须**: 所有用户输入必须验证和清理
- ✅ **必须**: 防止SQL注入、XSS攻击、CSRF攻击

### 认证授权
- ✅ **必须**: 所有API端点都需要JWT认证（除公开接口）
- ✅ **必须**: 使用 `require_role` 实现基于角色的权限控制
- ✅ **必须**: 敏感操作记录审计日志

---

## 🧪 测试规范

### 测试覆盖率要求
- **单元测试**: 覆盖率 ≥ 80% (强制)
- **集成测试**: 所有API端点必须有集成测试
- **E2E测试**: 关键业务流程必须有E2E测试

### 测试命名规范
```python
# 后端测试
def test_create_project_success():  # 测试成功场景
def test_create_project_unauthorized():  # 测试权限验证
def test_create_project_validation_error():  # 测试参数验证

# 前端测试
it('should render project list', () => {})
it('should handle API errors', () => {})
```

### 测试运行
```bash
# 后端测试（必须通过）
pytest tests/ -v --cov=backend --cov-report=term

# 前端测试（必须通过）
pnpm test
pnpm type-check
pnpm lint
```

---

## 📝 代码规范

### Git提交规范（Conventional Commits）
```bash
<type>(<scope>): <subject>

# 类型(type)
feat: 新功能
fix: Bug修复
docs: 文档更新
style: 代码格式化
refactor: 重构
perf: 性能优化
test: 测试
chore: 构建/工具链

# 示例
feat(projects): 添加项目管理API
fix(auth): 修复权限验证bug
docs(api): 更新API文档
```

### 代码注释规范
- Python: 使用docstring (Google风格)
- TypeScript: 使用JSDoc
- 复杂逻辑必须添加注释
- TODO注释必须包含负责人和日期

### 代码格式化（自动化）
```bash
# 后端格式化
black .
isort .

# 前端格式化
pnpm prettier --write .
```

---

## 🤖 AI助手开发约束

### 强制性原则
1. ❌ **禁止**: 全局性修复任务（如"修复所有拼写错误"）
2. ✅ **必须**: 任务明确指向具体文件或模块
3. ✅ **必须**: 执行前输出计划并等待确认
4. ✅ **必须**: 运行验证命令并报告结果
5. ❌ **禁止**: 泄露敏感信息到日志

### 开发流程约束
- ✅ **必须**: 先查看 `git status`/`git diff`
- ✅ **必须**: 使用工具发现问题（ESLint/pytest）
- ❌ **禁止**: 逐文件"肉眼修复"
- ✅ **必须**: 按"修改内容 + 影响文件 + 验证方式"格式输出

**详细约束**: 见 [CLAUDE.md](CLAUDE.md)

---

## 📚 规则文档索引

### Level 2: 领域规范文档（必读）
- **[API_RULEBOOK.md](docs/API_RULEBOOK.md)** - API开发总规则
- **[FRONTEND_DEVELOPMENT_RULES.md](docs/rule/FRONTEND_DEVELOPMENT_RULES.md)** - 前端开发规则总纲
- **[CLAUDE.md](CLAUDE.md)** - AI助手开发规范
- **[AGENTS.md](docs/rule/AGENTS.md)** - 协作者规范

### Level 3: 执行指南（开发时参考）
- **[AI_MODULE_DEVELOPMENT_WORKFLOW.md](docs/rule/AI_MODULE_DEVELOPMENT_WORKFLOW.md)** - 全栈模块开发流程
- **[UI_DESIGN_GUIDE.md](docs/rule/UI_DESIGN_GUIDE.md)** - UI设计指南
- **[INTERFACE_DEVELOPMENT_CHECKLIST.md](docs/rule/INTERFACE_DEVELOPMENT_CHECKLIST.md)** - 开发检查清单

### 完整文档索引
见 [docs/rule/README.md](docs/rule/README.md)

---

## ⚖️ 规则冲突解决机制

### 优先级顺序
1. **本文档 (.project-rules.md)** - 最高优先级
2. **领域规范文档** (API_RULEBOOK.md, FRONTEND_DEVELOPMENT_RULES.md等)
3. **执行指南和模板** (AI_MODULE_DEVELOPMENT_WORKFLOW.md等)
4. **参考文档** (技术文档、部署指南等)

### 冲突处理原则
- 当不同文档对同一问题有不同规定时，以优先级高的为准
- 当本文档未明确规定时，以领域规范文档为准
- 当发现规则冲突时，应提交issue由架构团队裁决

---

## 🚦 质量门禁（CI/CD）

### 代码提交前必须通过
```bash
# 后端检查
black . && isort .       # 代码格式化
flake8 .                 # 代码质量
mypy .                   # 类型检查
pytest tests/ -v         # 测试通过

# 前端检查
pnpm lint                # ESLint检查
pnpm type-check          # TypeScript检查
pnpm build               # 构建成功
```

### 代码审查必须检查
- [ ] 遵循代码规范
- [ ] 有适当的错误处理
- [ ] 有权限验证
- [ ] 有单元测试
- [ ] 无敏感信息泄露
- [ ] 性能符合要求

---

## 📊 性能指标要求

### 响应时间
- API P95响应时间: < 200ms
- 页面首次加载: < 3秒
- 页面交互响应: < 100ms

### 数据库
- 单表查询: < 50ms
- 关联查询: < 100ms
- 分页查询必须使用: 每页最多100条

### 前端
- 打包体积: < 500KB (gzip后)
- 首屏渲染: < 1.5秒
- Lighthouse分数: > 90

---

## 🔄 文档维护

### 更新原则
- 重大架构变更后必须更新本文档
- 每月1日定期审查文档有效性
- 发现规则冲突时立即修正

### 更新流程
1. 提交PR修改规则文档
2. 架构团队评审
3. 批准后更新版本号
4. 通知全体开发人员

### 版本管理
- 重大变更: 主版本号+1 (如 v1.0 → v2.0)
- 新增规则: 次版本号+1 (如 v1.0 → v1.1)
- 文字修正: 补丁版本号+1 (如 v1.0.0 → v1.0.1)

---

## 📞 规则咨询

### 遇到以下情况时
- 规则不明确或有歧义
- 不同规则文档冲突
- 需要制定新规则
- 规则执行遇到困难

### 联系方式
- 提交GitHub Issue (标签: `rules`, `question`)
- 联系项目架构团队
- 在团队会议上讨论

---

## ✅ 开发检查清单

### 开发前
- [ ] 阅读本文档和相关领域规范
- [ ] 理解业务需求和权限要求
- [ ] 确认技术方案符合规范

### 开发中
- [ ] 遵循代码规范和命名规范
- [ ] 实现权限控制
- [ ] 添加错误处理
- [ ] 编写测试用例

### 提交前
- [ ] 运行所有检查命令并通过
- [ ] 代码审查通过
- [ ] 文档更新完成
- [ ] Git提交信息规范

---

**重要提醒**:
1. 本文档是所有规则的总纲，具有最高优先级
2. 所有开发人员必须熟读本文档
3. 遇到规则冲突时，以本文档为准
4. 规则执行中的疑问应及时反馈

---

**文档版本**: v1.0
**最后更新**: 2025-11-16
**下次审查**: 2025-12-01
**维护责任人**: 项目架构团队
