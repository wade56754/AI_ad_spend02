# =============================================================================
# AI财务系统环境变量配置模板
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填入实际的配置值
# 3. 确保 .env 文件已添加到 .gitignore 中
# =============================================================================

# 数据库连接配置
# 格式: postgresql://username:password@host:port/database
# ⚠️ 请使用强密码，避免使用默认密码
DATABASE_URL=postgresql://your_username:your_secure_password@your_host:5432/your_database

# JWT配置
# ⚠️ 请使用64位以上的随机字符串，不要使用示例密钥
# 生成命令: openssl rand -base64 64
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-64-characters-long

# Supabase配置
# 从Supabase控制台获取这些值
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-key

# 环境配置
# 开发环境: dev | staging | production
ENV_NAME=development

# CORS配置
# 允许的前端域名列表，用逗号分隔
# 生产环境请设置为具体域名，不要使用通配符
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 应用配置
APP_NAME=AI Finance System
DEBUG=false
LOG_LEVEL=INFO

# 安全配置
# JWT token过期时间（分钟）
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# 数据库连接池配置
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=30

# 文件上传配置
MAX_FILE_SIZE=10485760  # 10MB
UPLOAD_PATH=./uploads

# 邮件配置（可选）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Redis配置（可选，用于缓存）
REDIS_URL=redis://localhost:6379/0

# 监控配置（可选）
SENTRY_DSN=your-sentry-dsn
PROMETHEUS_PORT=9090

# API限流配置
API_RATE_LIMIT=100
API_RATE_WINDOW=60  # 秒

# 加密配置
# 数据加密密钥，用于敏感数据加密
ENCRYPTION_KEY=your-32-character-encryption-key

# 备份配置
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *  # 每天凌晨2点
BACKUP_RETENTION_DAYS=30

# =============================================================================
# 安全提醒：
# 1. 永远不要将包含真实密钥的 .env 文件提交到版本控制系统
# 2. 定期轮换密钥和密码
# 3. 在生产环境使用强密码和随机密钥
# 4. 启用数据库SSL连接
# 5. 定期备份配置文件
# =============================================================================